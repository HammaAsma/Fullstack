# Documentation technique — React Week 2

Semaine 2 : **routing** (React Router) et **mini-blog**. Ce dossier contient les exercices de routes et le projet blog.

---

## 1. Exo_Route — outlets

Exercices de routing : structure **admin**, **main**, **Users**, pages d’**erreurs**.

### Lancement

- Dans `Exo_Route/outlets` : `npm install` puis `npm run dev`.

---

## 2. Mini-blog

Blog avec **React Router**, **pages** (`src/pages`), fonctionnalités **auth** (`src/features/auth`), données dans `src/data` et `src/features`.

### Stack

- **React**, **React Router** (routes, outlets, layout). Structure : `src/pages`, `src/features`, `src/routes`, `src/data`, `src/assets`.

### Lancement

- Dans `mini-blog` : `npm install` puis `npm run dev` (port 5173).

---

*Formation Full Stack — React Week 2*
