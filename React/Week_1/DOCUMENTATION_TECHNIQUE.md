# Documentation technique — React Week 1

Semaine 1 : bases React (composants, état, hooks). Ce dossier contient les **exercices** et le **mini-project api-users**.

---

## 1. Exo — my-project-react

Projet d’exercices React : composants **Ex01**, **Ex02**, **Ex03** dans `src/components`.

### Lancement

- Dans `Exo/my-project-react` : `npm install` puis `npm run dev`.

---

## 2. Mini-project — api-users

Application React qui consomme une API utilisateurs : liste, **hooks** (useState, useEffect), composants, assets.

### Stack

- **React**, **Vite**. Pas de routing avancé (semaine 2).

### Lancement

- Dans `mini-project/api-users` : `npm install` puis `npm run dev` (port 5173).

---

*Formation Full Stack — React Week 1*
